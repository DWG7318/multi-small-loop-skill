interface:
  display_name: "Multi Small Loop"
  short_description: "Oversee parallel Checker-Worker loops"
  default_prompt: "Use $multi-small-loop-skill to split this project into independent Checker-Worker small loops, supervise them periodically, wake stalled Checkers, and continue until every planned CELL is accepted."
