interface:
  display_name: "Small Loop Skill (slk)"
  short_description: "Parallel loops with timed oversight"
  default_prompt: "Use $small-loop-skill (slk) for parallel Checker-Worker loops with timed Supervisor oversight and evidence-based acceptance."
