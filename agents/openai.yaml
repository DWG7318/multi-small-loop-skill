interface:
  display_name: "Multi Small Loop (MSLK)"
  short_description: "Dependency-ready parallel loops with oversight"
  default_prompt: "Use $multi-small-loop-skill (MSLK) only after proving multiple fresh Workers are necessary, acceptance-independent, and immediately startable; pair each Worker with one Checker, otherwise use SLK."
