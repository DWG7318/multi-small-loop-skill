interface:
  display_name: "Multi Small Loop (MSLK)"
  short_description: "Persistent Block workers with timed oversight"
  default_prompt: "Use $multi-small-loop-skill (MSLK) to derive persistent Worker/Checker pairs from independent Blocks, assign one or more GO and bounded CELLs to each pair, and run timed Supervisor oversight with evidence-based acceptance."
